To use this module, you need to:

1.  Go to *Connectors \> Queue \> Jobs*, and check the correct enqueuing
    of the tasks.
2.  Check on each menu the resulting imported records (Customers, Sales
    Orders...)

## Test dependencies

Extra libs are required to run the tests: \* `vcrpy` \* `freezegun`
