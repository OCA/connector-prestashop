<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data noupdate="1">

        <record id="excep_order_wrong_total_amount" model="sale.exception">
            <field name="name">Sale order have a wrong amount.</field>
            <field name="description">The total amount is different than the prestashop total payment.</field>
            <field name="sequence">20</field>
            <field name="model">sale.order</field>
            <field name="code">if abs(order.amount_total - order.prestashop_bind_ids[0].total_paid) >= 0.01:
    failed = True</field>
            <field name="active" eval="True"/>
        </record>

    </data>
</openerp>
